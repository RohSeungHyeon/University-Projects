<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="teamproject.accountbook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity"
            android:theme="@style/Splash">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".addCategory"
            android:theme="@android:style/Theme.Dialog" />
        <activity
            android:name=".ModifyCategory"
            android:theme="@android:style/Theme.Dialog" />
        <activity
            android:name=".PopUpActivity"
            android:theme="@android:style/Theme.Dialog" />
        <activity
            android:name=".SelectFilterOption"
            android:theme="@android:style/Theme.Dialog" />
        <activity
            android:name=".FilterResult"
             />
        <activity android:name=".addAccountBook"
            android:theme="@android:style/Theme.Dialog"/>
        <activity android:name=".ABViewActivity"/>
        <activity android:name=".MainActivity"/>
    </application>

</manifest>